{% extends "otpet/index.html" %}
{% block title %}Health Extension Worker {{ location }}{% endblock %}

{% block content %}
<h2>{{ location }}</h2>

<h3>Reporters</h3>
<table width="100%"> 
    <thead>
        <tr>
            <th>Code</th>
            <th>Name</th>
            <th>MRDT Stock</th>
            <th>Today's Reports</th>
        </tr>
    </thead>
    <tbody>
        {% for location in locations %}
            <tr>
                <td><a href="/otpet/location/{{ location.obj.id }}">{{location.alias}}</a></td>
                <td>{{location.obj}}</td>
                <td>{{location.stock}}</td>
                <td>{{location.reports}}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
{% endblock %}
